# 决策日志 · MindPrint UX

> 每条 UX 决策、变更、创意工具产物的归属、用户偏好选择都登记在此。
>
> 格式：`YYYY-MM-DD HH:MM` · 决策摘要 · 上下文 / 理由

---

## 2026-05-28 · 工作区初始化

- **意图**：Create（从 PRD 推进到 UX）
- **Sources 引用**（不复制）：
  - `_bmad-output/planning-artifacts/briefs/brief-my-bmad-app-2026-05-28/brief.md`（status: final）
  - `_bmad-output/planning-artifacts/prds/prd-my-bmad-app-2026-05-28/prd.md`（status: final）
  - `_bmad-output/planning-artifacts/prds/prd-my-bmad-app-2026-05-28/addendum.md`
- **工作模式**：Fast path（批量问 gaps + 我直接草拟双 spine，[ASSUMPTION] 标记推断）
- **授权**：alex 全部授权 + infer-and-confirm
- **沟通语言**：中文（来自 BMM config）
- **文档输出语言**：中文（双 spine 主体中文，token 名称英文）

## 2026-05-28 · Fast path · Discovery 批量答复

- **整体视觉调性**：A · 档案馆纸质感（暖白 + 衬线字体 + 极少强调色）
- **Brand voice**：克制工具（不寒暄、不智能化煽情、陈述性收束）
- **OQ-1 产品名**：**保留 MindPrint**（OQ-1 已关闭，进入 logo / wordmark 设计）
- **暗色模式**：跟随系统（DESIGN.md tokens 同时定义 light/dark 两套）

## 2026-05-28 · DESIGN.md 草拟完成

- 风格定位 **"Editorial Archive"** —— 视觉给暖意（档案馆纸质感）/ voice 克制（不抢戏）/ "视觉温度 + voice 克制" 的双层张力
- **核心 token**：
  - Surface `#FAF8F3` 暖白 + `#1B1C19` 墨黑 + `#735C41` 旧书皮深棕 primary + `#B84A39` 衰红（极少使用，全屏 < 50px²）
  - 中文优先字体：思源宋体（中文标题/正文）+ Newsreader（英文衬线 fallback）+ 思源黑体 / Inter（UI 元素，比例 < 15%）+ JetBrains Mono（元数据时间戳）
  - 圆角软方（默认 4px，卡片/modal 8px）
  - 桌面 margin 56px / 移动 20px / editorial-gap 64px
- **关键 component 决策**：
  - Card 默认阴影几乎不可见，hover 微抬起（橡棕染色阴影），1px dust ghost outline 是视觉签名
  - **禁用 hover overlay 按钮**（PRD D28 承接，管理动作集中 Full Render）
  - Month Divider 用 display-lg 衬线大字 + dust 横线 + editorial-gap 上下间距——thesis "思维演进回看" 的视觉锚点
  - 缩略预览区 pointer-events 禁用（卡片整体单击进 Full Render）
  - Top Chrome 顶部条结构：← / 标题+时间 / ⟨⟩上一下一 / ⋯更多
- **Don't 列表**：禁用 emoji、不用插图、不渐变、不玻璃拟态、卡片不显示原始文件名、单击不悬浮预览

## 2026-05-28 · EXPERIENCE.md 草拟完成

- **IA**：5 个 surface（登录 / 时间线 / Full Render / 归档 modal / 删除确认 modal）。无侧边栏 / 无 Settings 页
- **Surface closure 校验**：UJ-1 → 时间线 + Full Render；UJ-2 → 归档 modal + Full Render；UJ-3 → 时间线 + Full Render。**所有 UJ 都有归宿，所有 surface 都有 UJ 落点** ✅
- **Voice 表 8 行 Do/Don't** —— 克制工具陈述句、无 emoji、无副词堆砌、用句号收束
- **Interaction Primitives**：
  - 键盘核心：Esc / ←→（仅 Full Render 上一下一）/ Enter / Tab
  - **故意不做的**：⌘K（无搜索）/ vim 风格快捷（无多 surface 切换）/ 数字键跳转
  - **移动端退化**：拖拽上传不可用（用系统文件选择器替代），hover 移除，顶部 chrome 紧凑化
- **3 Key Flows 完整写出**（找回 / 归档 / 演进回看），每条含 climax beat + 失败分支
- **State Patterns 12 行**——含跨设备并发归档（V1 不实时同步）、缩略预览失败、会话过期等边缘场景
- **Accessibility Floor**：WCAG 2.2 AA、键盘可达全 surface、屏幕阅读器 surface 宣告、reduced-motion 支持
- **关键 [ASSUMPTION]**（待 alex 在 finalize 阶段确认）：
  - 标题 inline edit 用 Notion 风格（单击进入、blur 保存、Esc 撤销）
  - 移动端拖拽上传退化为系统文件选择器
  - 会话过期后重定向到登录 + 登录后回到原 URL
  - 跨设备并发归档 V1 不实时同步（手动刷新）

---

## 2026-05-28 · PWA 时机讨论 · 决定走 prototype 路线

- **alex 提出**：使用 Next.js + mock 数据做一个 PWA 交互原型
- **路线性质**：**不在 BMAD 主工作流内**——这是探索性 prototype，目的是
  - 学 PWA + Next.js 全栈技能（Service Worker / Manifest / Cache API / IndexedDB）
  - 验证 PWA 在 MindPrint 体验上的实际价值（主屏 / 离线 / 桌面安装）
  - **不影响 V1 范围**——PRD §6.2 "V1 暂不做" 保持
- **后续决策路径**：
  - prototype 跑通后，回过头决定是否升格 V1 (sprint change → `bmad-correct-course`) / v1.1 / 永远不做
  - prototype 的发现会作为 M3 retro 的输入数据
- **文档一致性修复**：EXPERIENCE.md "PWA V1 永久排除" → "V1 不做 PWA，未来视使用情况评估"（与 PRD §6.2 措辞一致）

---

## 2026-05-28 · Finalize 完成 · status → final

**Finalize 7 步执行情况**：

1. **Spines distilled** ✅ — Fast path：alex 给视觉调性 + voice + 产品名 + 暗色后，我直接草拟双 spine，[ASSUMPTION] 标记推断项
2. **Inputs reconciled** ✅ — `reconcile-sources.md` 子代理识别 24 gap（1 critical + 5 high + 13 medium + 7 low）
3. **Reviewer Gate** ✅ — `review-rubric.md` 子代理给 PASS 裁决（8 维度 6 STRONG + 2 ADEQUATE）
4. **Open items triaged** ✅ — critical/high 全部 autofix（2 critical + 4 high）
5. **Key-screen mocks rendered** ✅ — `mockups/timeline-mock.html` + `mockups/full-render-mock.html`
6. **Mock coverage confirmed** ✅ — alex 选 Timeline + Full Render；归档 modal / 删除 modal / 空状态由 dev 阶段从 spine + Timeline mock 推导
7. **Layout extracted, artifacts promoted** ✅ — `.working/` mocks 升格至 `mockups/`；DESIGN.md + EXPERIENCE.md 加 inline 引用 + "spine 胜冲突" 声明
8. **Polish** ⏭️ — 关键 prose 与结构 fix 已在 step 4 完成；deep editorial review 跳过（alex 之前 PRD 阶段对细微 polish 不敏感，节省成本）
9. **External handoffs** ⏭️ — `external_handoffs` 配置为空
10. **Close** ✅ — DESIGN.md + EXPERIENCE.md status → final，version 1.0，finalized 2026-05-28

**alex 最后两决策**：
- **暗色 V1**：升格为正式承诺（[ASSUMPTION] 移除）。架构 / dev 必须实现 light/dark 双模
- **额外 mock**：补 Full Render；归档 modal / 删除 modal / 空状态 spine-only 自然

**产物清单**：
- `DESIGN.md`（status: final）
- `EXPERIENCE.md`（status: final）
- `.decision-log.md`（本文件）
- `reconcile-sources.md`（子代理对齐报告）
- `review-rubric.md`（子代理质量评审）
- `mockups/timeline-mock.html`（关键屏 mock）
- `mockups/full-render-mock.html`（关键屏 mock）

---

## 2026-05-28 · Finalize · 3 并行子代理回报

- **Reviewer rubric**：OVERALL **PASS**。8 维度 / 6 STRONG + 2 ADEQUATE。**最大暗礁**：OQ-8 技术机制硬写入 DESIGN.md（违反"spine 不锁机制"原则）+ breakpoint 不一致。
- **Reconcile**（24 gap，1 critical + 5 high + 13 medium + 7 low）：
  - **CRITICAL**：NFR-2 资源层（下载 URL 签名 / 短时效）UX 端缺失 → 已加入 Component Patterns Download Link 行
  - **CRITICAL**：DESIGN（640/1024/1440）与 EXPERIENCE（768/1024/1440）breakpoint mismatch → 统一为 768/1024/1440（主流 Tailwind 标准）
  - **HIGH**：voice 缺"已发生过实际丢失 → 安心" 情绪承接 → 加 voice 表两行
  - **HIGH**：暗色模式是 V1 单向新增（brief/PRD 未明示）→ 加 [ASSUMPTION] 标记，alex 在 finalize 决定
  - **HIGH**：OQ-8 技术机制硬写入 → 改为抽象表述
  - **HIGH**："画廊式翻看"漫游/驻足动词在交互节奏无承接 → Interaction Primitives hover 描述加"漫游中的驻足提示"
- **Timeline mock**：`.working/timeline-mock.html`，29.7KB，9 张语义化卡片跨 3 月份分布，含 light/dark + reduced-motion 退化。
- **medium/low autofix 暂未做**：alex 模式可接受默认延后到 polish 阶段批量过；reviewer + reconcile 完整报告在文件里

## 待 alex finalize 最终确认

- **暗色模式去留**：[ASSUMPTION] 已标，需 alex 显式 保留 V1 / 砍至 v2 / 让我决定
- **其他 surface mock**：alex 选了仅 Timeline。Full Render / 归档 modal / 删除 modal / 空状态 是否需要 mock（finalize 阶段最后窗口）

---

## PRD 已锁定、UX 直接承接的约束

- **Form-factor**：web 响应式（任意设备浏览器）—— brief V1 硬约束 + FR-5 响应式 + NFR-3 浏览器覆盖（Chrome / Safari / Firefox 桌面 + iOS Safari / Android Chrome 移动）
- **核心视觉承诺**：§4.2 卡片网格 + HTML 内容缩略预览 + "**画廊式翻看**" 体感隐喻——UX 必须兑现
- **§4.3 完整渲染**：全屏 + 上一/下一导航 + 顶部 chrome（标题 + 归档时间 + 返回时间线）
- **NFR-1 沙箱化**、**NFR-2 私有访问**——交互层不能为安全牺牲体验
- **§4.4 管理动作集中在 Full Render 视图**——时间线卡片不堆按钮
- **3 个 UJ** 命名：UJ-1 找回 / UJ-2 归档 / UJ-3 思维演进回看

## 待 UX 阶段解决的 PRD 开放项

- **OQ-1**：产品名最终确认（UX 必须解决——logo / wordmark 需要）
- **OQ-8**：缩略预览生成机制（PRD 倾向方案 a 缩放 iframe；UX 主要承接其交互行为不锁机制）

---
