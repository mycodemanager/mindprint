# Prose Review — addendum.md

**Reader type:** humans（技术）
**Style baseline:** Microsoft Writing Style Guide + addendum 自身的"terse / 技术手册"调性
**Scope:** 仅修在**clarity-impeding**的问题；保留作者一致的"破折号 + 内联标签"风格

---

| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| 承接 PRD 主文档之外、不属于 PRD 但对下游（架构 / UX / 实现）有价值的信息：技术倾向、机制选项、被讨论但未入 PRD 的细节、源材料中提及但 PRD 不直接承接的内容。 | 承接 PRD 主文档之外、对下游（架构 / UX / 实现）有价值的信息：技术倾向、机制选项、被讨论但未入 PRD 的细节。 | 删除"不属于 PRD 但"——"主文档之外"已经隐含此义，重复；并删除冗余的"源材料中提及但 PRD 不直接承接的内容"——与"被讨论但未入 PRD 的细节"同义。提升信息密度。 |
| 与 brief 中"通用云服务（Vercel/Netlify/Cloudflare 等）+ 托管 DB"的部署约束高度契合；与 alex 前端工程师背景匹配；App Router + Route Handlers 能在单一代码库内承载上传 API、时间线 SSR、渲染页面。 | 与 brief 部署约束（通用云服务 + 托管 DB）契合；与 alex 前端背景匹配；App Router + Route Handlers 可在单一代码库内承载上传 API、时间线 SSR、渲染页面。 | "高度契合"改"契合"——hedging 程度词不增加信息（MS 风格指南：直陈胜过强调）；"前端工程师"改"前端"——上下文已知 alex 是工程师；"能"改"可"——句中已含"在单一代码库内"的能力声明，"可"更精准。 |
| MindPrint 渲染的是用户输入的任意 HTML。即使唯一用户是 alex 自己，AI 生成的 HTML 仍属"untrusted input"——可能含 `<script>`，未沙箱化时能读取应用 cookie / localStorage / 越权调用应用 API。 | MindPrint 渲染的是用户输入的任意 HTML。即使唯一用户是 alex，AI 生成的 HTML 仍属"untrusted input"——可能含 `<script>`，未沙箱化时可读取应用 cookie / localStorage、越权调用应用 API。 | "alex 自己"删除"自己"（冗余反身代词）；"能"改"可"（与同段同类动作一致）；中段"读取……越权调用……"用顿号连接而非斜杠——斜杠后接动词短语易误读，顿号更清晰。 |
| Supabase Storage / R2 / Vercel Blob 上的数据是不是真的算"alex 自己掌控"？取决于供应商绑定深度、迁出难度、文件存储格式是否标准。 | Supabase Storage / R2 / Vercel Blob 上的数据是否真的算"alex 掌控"？取决于供应商绑定深度、迁出难度、存储格式是否标准。 | "是不是"改"是否"（正式技术文档调性）；"alex 自己掌控"改"alex 掌控"（与上一条同理，避免"自己"作冗余反身代词）；"文件存储格式"改"存储格式"——上下文谈对象存储已限定语义。 |
| 例如把对象存储放到 alex 自己的 S3 / R2 桶（用户级账号，可随时全量迁出），vs 直接用 Supabase Storage（集成简单但迁出门槛更高）。 | 例如把对象存储放到 alex 的 S3 / R2 桶（用户级账号，可随时全量迁出），对比直接用 Supabase Storage（集成简单但迁出门槛更高）。 | "alex 自己的"删除"自己"；"vs"改"对比"——中英混排正式技术文档建议避免常用对照标点的 ASCII 缩写（除非已是 PRD 全局风格——此处 addendum 仅出现一次，建议中文化）。 |
| 能力要求是"只有 alex 能访问"，brief 明确"无注册流程、无社交"。 | 能力要求是"只有 alex 可访问"，brief 明确"无注册流程、无社交"。 | "能访问"改"可访问"——与同文档下方"任意设备的浏览器都能访问"以外的多数表达保持"可"统一（addendum 内已 7 处用"可"，仅此处用"能"，统一术语降低读者切换成本）。 |
| 能力要求是"卡片视觉上能看出原 HTML 大致长什么样"，**机制不锁**。 | 能力要求是"卡片视觉上可辨识原 HTML 大致外观"，**机制不锁**。 | "能看出……大致长什么样"过于口语化，对技术文档读者偏松弛；"可辨识……大致外观"更紧、且与 PRD A5 中"视觉上能辨识"措辞对齐。**注意此修改需 alex 确认**——若有意保留口语温度则回退。 |
| 前端用 `<iframe sandbox>` 加载 Entry 的 HTML，CSS `transform: scale()` 缩放到卡片尺寸。**优点**：实现最轻、缩略图与渲染共用一份资产、HTML 改了卡片自动新。 | 前端用 `<iframe sandbox>` 加载 Entry 的 HTML，CSS `transform: scale()` 缩放到卡片尺寸。**优点**：实现最轻；缩略图与渲染共用一份资产；HTML 更新时卡片自动同步。 | "HTML 改了卡片自动新"——口语化省略主谓关系导致读者要补一拍：谁改、什么自动什么。规范化为"HTML 更新时卡片自动同步"。同时把"、"改";"——优点列表内含三条独立分句，分号更明确分项边界。 |
| 每张卡片都是真实 DOM 渲染，N 百卡片同屏性能成问题；可用 IntersectionObserver 做视口懒渲染缓解。 | 每张卡片都是真实 DOM 渲染，N 百张卡片同屏时性能成问题；可用 IntersectionObserver 做视口懒渲染缓解。 | "N 百卡片"→"N 百张卡片"——量词补全，避免读者短暂语法分析；增加"时"明确条件——"同屏性能成问题"易读成名词短语，"同屏时性能成问题"明确为条件句。 |
| 上传时 server side 用 Puppeteer / Playwright 截一张缩略图存进对象存储，卡片直接展示静态图。**优点**：浏览端零渲染负担。**缺点**：架构复杂度大幅上升（serverless 跑 headless Chrome 不一定可行，需要单独的截图 worker），首个全栈项目复杂度过重。 | 上传时服务端用 Puppeteer / Playwright 截一张缩略图存进对象存储，卡片直接展示静态图。**优点**：浏览端零渲染负担。**缺点**：架构复杂度大幅上升（serverless 跑 headless Chrome 未必可行，需独立的截图 worker），对首个全栈项目复杂度过重。 |  "server side"→"服务端"（中文文档中英混排无必要；同段其他位置无相同 ASCII 术语）；"不一定可行"→"未必可行"（更简洁正式）；"需要单独的"→"需独立的"（同义紧缩）；末句补"对……过重"使主语清晰。 |
| **方案 c：首屏 HTML 文本提取**——只展示标题 + HTML 内的文本摘要 / 首段，不渲染缩略图。**优点**：实现极轻。**缺点**：违背"视觉上能看出长什么样"的体验目标——更像是"美化列表"而非真正的视觉档案。 | **方案 c：首屏 HTML 文本提取**——只展示标题 + HTML 内的文本摘要 / 首段，不渲染缩略图。**优点**：实现极轻。**缺点**：违背"视觉上可辨识外观"的体验目标——更像"美化列表"而非视觉档案。 | "视觉上能看出长什么样"——同前条统一改为"视觉上可辨识外观"（保持术语一致）；"更像是"→"更像"（去冗余助词）；"真正的视觉档案"→"视觉档案"——"真正的"是修辞强调，不增加信息（MS 风格：去掉空程度词）。 |
| 缩放 iframe + 视口懒渲染 + 单卡片首屏渲染上限（例如卡片内的 `<script>` 禁用，仅渲染静态布局），在 hobby 复杂度内最贴近 PRD 体验目标。 | 缩放 iframe + 视口懒渲染 + 单卡片首屏渲染上限（例如禁用卡片内的 `<script>`，仅渲染静态布局），在 hobby 复杂度内最贴近 PRD 体验目标。 | "卡片内的 `<script>` 禁用"——倒装的被动结构，对读者额外解析成本；改为主动语态"禁用卡片内的 `<script>`"，与同段其他动词主动语态一致（"缩放 / 渲染"）。 |
| alex 的产出经常是 AI 生成的"单页 .html"——但也可能是含外链 CDN（fonts、图床）的 HTML，离线后样式可能塌。 | alex 的产出常是 AI 生成的"单页 .html"——但也可能含外链 CDN（fonts / 图床），离线后样式可能塌。 | "经常是"→"常是"（紧缩）；"含外链 CDN……的 HTML"——"的 HTML"在此句末多余，前文已是 .html 语境；同时把括号内顿号改斜杠以与文档其他位置（"fonts、图床"vs上文"Vercel/Netlify/Cloudflare"）的对照风格一致（斜杠用于同类项并列）。**注意**：此项作者可自行决定保留顿号——非 clarity 问题，仅风格统一。 |
| V1 暂不主动处理资源内联化。若 alex 在使用过程中发现"归档后样式塌"频繁发生，再升级到 v2。**Open Question 候选**。 | V1 不主动处理资源内联化。若 alex 在使用过程中发现"归档后样式塌"频繁发生，再升级到 v2。**Open Question 候选**。 | "暂不"→"不"——addendum 整体已是 V1 视角，"暂"是冗余的时间限定（V1 范围内即"不"，是否在 v2 处理本句下文已明示）。MS 风格："avoid throat-clearing"。 |
| "Entry 的删除"——叫"删除（Delete）"还是"归档撤销（Unarchive）"还是"丢弃（Discard）"？语义不同（删除=物理移除，撤销=保留文件回到 ~/Downloads）。**Discovery 期间需确认 alex 想要哪种语义**。 | "Entry 的删除"——叫"删除（Delete）"、"归档撤销（Unarchive）"还是"丢弃（Discard）"？三者语义不同（删除 = 物理移除；撤销 = 保留文件回到 ~/Downloads；丢弃 = 待定）。**Discovery 期间需确认 alex 想要哪种语义**。 | 原句"还是……还是……"重复读起来卡顿；列表化为顿号 + "还是"二者只取后者；同时补出"丢弃 = 待定"——原句仅给"删除""撤销"两种语义但题目列三个，"丢弃"语义缺失会让读者推断起来更费力。**注意**：本节若按结构 review 整体 CUT，可忽略此修改。 |

---

## Summary

- **Total fixes:** 14
- **Categories:** 
  - 冗余精简（"自己"反身代词 / "经常是"等程度词）: 4 处
  - 术语一致（"能/可"统一 / "视觉上能看出长什么样"→"可辨识外观"）: 3 处
  - 中英混排规范（"server side"→"服务端" / "vs"→"对比"）: 2 处
  - 语法明确（条件句"同屏时" / 主语补全 / 主动语态）: 3 处
  - 列表分项清晰（分号 vs 顿号）: 2 处

- **Author voice preserved:** 破折号、内联粗体标签、"~~~"+ "**优点 / 缺点**"列表模式、中文技术文档调性均未变。
- **Out of scope (not fixed):** 作者一致使用的 "alex" 全小写（约定）、中英混合标点（部分保留原风格）。
