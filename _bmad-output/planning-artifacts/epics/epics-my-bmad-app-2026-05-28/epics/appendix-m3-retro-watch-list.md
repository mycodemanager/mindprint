# Appendix · M3 Retro Watch List

> PRD §9 列出的 8 个 Open Questions——架构阶段已关闭 1（OQ-8）+ 部分承接 1（OQ-7）+ 显式延后 6 个。dev 阶段**不应**为这些预留脚手架；M3 retro 时按下表触发条件分诊。

| OQ                  | 当前 V1 立场               | 架构阶段处理                                                                | M3 触发条件                                         |
| ------------------- | ---------------------- | --------------------------------------------------------------------- | ----------------------------------------------- |
| **OQ-1 产品名称最终确认**   | 工作名 MindPrint          | ⏸ 留 alex 在上线前定                                                        | 上线前必决（否则品牌资产无法沉淀）                               |
| **OQ-2 撤销归档单步动作**   | 下载 + 删除分两步             | ⏸ V1 不做                                                               | alex 实际使用中产生"分两步太麻烦"的不便                         |
| **OQ-3 重复内容检测**     | 不做去重，独立 Entry          | ⏸ schema 不预留 content\_hash 字段                                         | alex 发现过"已归档过又传了一次"                             |
| **OQ-4 HTML 资源完整性** | 不主动内联外链                | ⏸ 上传链路不预留 inline 化扩展点                                                 | "打开旧 Entry 发现样式塌了"频次高                           |
| **OQ-5 排序偏好持久化**    | URL state 不持久化         | ⏸ V1 每次进入回默认倒序                                                        | alex 频繁切到正序                                     |
| **OQ-6 沉浸全屏模式**     | Full Render 始终带 chrome | ⏸ V1 不做                                                               | alex 反复想"专心看一份 Entry"                           |
| **OQ-7 全量数据导出**     | 仅单条下载（FR-7）            | 🟡 **部分承接**——周期备份脚本生成完整 db dump + R2 inventory + alex Mac 周期同步 ≈ 迁出兜底 | alex 决定换平台 / 部署目标变更，**优先级最高**（数据所有权立场核心）        |
| **OQ-8 缩略预览机制**     | PRD A5 倾向方案 a          | ✅ **关闭**——锁定方案 a（缩放 iframe + 视口懒渲染）；**禁止静默降级**到方案 c                   | 50/100 条规模下方案 a 性能不足 → 按 FR-4 可验收条件**触发 PM 回顾** |

**架构阶段悬而未决 · 留给 M3 retro 评估**：

- **Magic Link 邮件服务降级路径**——Resend 故障时无登录兜底（缓解：本地 Mac 周期备份仍有数据）
- **测试框架引入**（Vitest / Playwright）——hobby 项目按需在测试策略章节决策
- **Sentry 类错误追踪**——hobby 暂用 Vercel 日志面板

**架构验证发现的 Important Gaps**（来自 architecture-validation-results.md）：

1. Magic Link 邮件服务降级路径未定义
2. 性能预算未对原型实测（Story 4 完成后做一次 50 条种子实测）
3. Tailwind tokens 与 DESIGN.md 桥接细节将在 Story 7 起手时给出

